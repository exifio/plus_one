# AGENTS.md

## 프로젝트 목적
- 이 프로젝트는 GS25/CU 1+1·2+1 보관상품의 **판매자 공급 가설을 검증하는 작은 MVP**다.
- 완성형 C2C 플랫폼이 아니라 빠른 외부 테스트가 목적이다.
- 현재 필요하지 않은 미래 확장용 구조는 만들지 않는다.
- 판매자 화면에서는 `실험`, `MVP`, `가설 검증` 같은 내부 용어를 노출하지 않는다.
- 정상적인 판매 신청은 운영자가 직접 구매를 진행한다. 사용자에게 `가능한 경우 구매`, `검토 후 결정`처럼 불확실하게 표현하지 않는다.

## 문서 역할
현재 작업에 필요한 문서만 읽는다.
- 제품 범위·사용자 흐름·판매 희망금액 규칙·실험 데이터: `docs/PRD.md`
- UI/디자인 규칙: `docs/DESIGN-SYSTEM.md`
- 현재 작업과 진행 상태: `docs/TASKS.md`
- Backend/Auth/RLS: Frontend 승인 후 생성할 `docs/BACKEND.md`
- 설치·실행·배포: 실제 환경이 정해진 후 작성할 `README.md`

같은 규칙을 여러 문서에 반복하지 않는다. 상세 내용은 해당 책임 문서를 참조한다.

## 개발 순서
1. Mock 기반 Frontend
2. Frontend Phase 검증
3. 사용자 Frontend 승인
4. `docs/BACKEND.md` 작성
5. Backend
6. Integration
7. QA
8. Deploy

사용자가 순서를 변경하지 않는 한 Frontend 승인 전에 Backend 구현을 시작하지 않는다.

## Task 작업 규칙
- 작업 시작 전 `docs/TASKS.md`의 `현재 작업`을 확인한다.
- Task 하나는 보통 화면 하나, 사용자 행동 하나, 또는 집중된 기능 하나로 잡는다.
- 필요한 파일 탐색은 시작 시 한 번의 묶음으로 수행한다. 같은 파일이나 상태를 이유 없이 반복 조회하지 않는다.
- 요구사항과 수정 위치를 파악했으면 추가 계획 설명보다 구현을 우선한다.
- 현재 Task에 필요하지 않은 리팩터링, 스타일 정리, 설정 변경, 도구 추가는 하지 않는다. 발견한 개선점은 현재 작업을 막지 않으면 건드리지 않는다.
- 기존 도구로 구현·검증할 수 있으면 새 개발/검증 패키지를 설치하지 않는다.
- Task 중 완료 조건에 없던 Reviewer, QA, 품질 게이트를 임의로 추가하지 않는다.
- Task를 실제로 완료하면 별도 지시 없이 같은 작업 안에서 `docs/TASKS.md`를 갱신한다.
- 완료된 Task는 `[x]`, 막힌 Task는 `[!]`로 표시하고 다음 작업을 갱신한다.
- 완료하지 않은 작업을 완료로 표시하지 않는다.
- 병렬 작업 시 각 작업자는 자신의 변경 범위만 검증하고, 전체 통합 검증과 `docs/TASKS.md` 최종 갱신은 메인 작업자가 수행한다.
- 다른 작업자가 변경 중인 영역을 불필요하게 탐색하거나 수정하지 않는다.
- 구체적인 필요와 사용자 승인 없이 `PLAN`, `TEST-PLAN`, `SECURITY`, `ARCHITECTURE`, `DATA-MODEL` 같은 추가 문서를 만들지 않는다.

### Task 중단·보고 조건
다음 상황에서는 작업 범위를 확장하지 말고 현재 상태와 필요한 다음 조치만 보고한다.
- 현재 Task와 무관한 파일이나 기능을 수정해야 하는 경우
- 새로운 개발/검증 도구 설치가 필요해진 경우
- 같은 검증을 실패 원인 없이 2회 이상 반복하려는 경우
- 다른 병렬 Task의 미완성 변경 때문에 검증이 막힌 경우
- 구현보다 탐색·검토·QA 시간이 더 커지기 시작한 경우
- 완료 조건에 없던 추가 Reviewer/QA가 필요하다고 판단한 경우

## 검증 수준
### Task 수준
변경한 기능에 필요한 최소 검증만 수행한다.
- 구현 후 `lint`, `typecheck`, 관련 테스트 등 빠른 검증을 우선하며, 성공한 동일 검증은 코드가 다시 바뀌지 않았다면 반복하지 않는다.
- UI/스타일: 변경 화면 직접 확인
- Form: 변경한 입력과 validation 확인
- 가격/비즈니스 규칙: 해당 단위 테스트
- 작은 리팩터링: 필요한 경우 관련 테스트 또는 typecheck
- 검증 실패 시 원인을 수정한 뒤 실패한 검증만 다시 수행한다.

모든 Task마다 전체 E2E, 전체 보안 검사, 전체 프로젝트 검증을 반복하지 않는다.

### Playwright 규칙
- 모든 코드 변경마다 Playwright를 실행하지 않는다. 실제 브라우저 동작이나 화면 결과 확인이 필요할 때만 사용한다.
- 단순 텍스트·주석·문서·설정 변경, 내부 코드 정리, 브라우저 동작에 영향 없는 타입 수정, 경미한 스타일 수정에는 원칙적으로 생략한다. UI가 깨질 가능성이 높을 때만 관련 화면을 확인한다.
- 새 화면/주요 컴포넌트, 레이아웃 구조, 버튼·링크·모달, 폼 입력·제출, 라우팅, 화면 상태 변화, 브라우저에서만 재현되는 문제에는 필요한 경우 사용한다.
- 개별 Task 완료 시에는 해당 Task와 직접 관련된 핵심 사용자 흐름만 검증한다.
- 전체 서비스를 탐색하지 않고 변경된 페이지·시나리오만 확인한다. 가능하면 특정 테스트 파일, 페이지, 시나리오만 실행한다.
- 개발 단계에서는 기본적으로 Chromium 하나만 사용한다. 필요하지 않은 브라우저, screenshot, trace, video는 추가하지 않는다.
- 개발 서버가 이미 실행 중이라면 가능한 범위에서 재사용한다.
- Playwright가 성공했다면 이유 없이 같은 검증을 반복하지 않는다.
- 실패했다면 원인을 수정한 뒤 실패한 시나리오만 재검증한다. 성공 후 해당 기능 코드를 다시 변경한 경우에만 재실행한다.

### Phase 수준
- Frontend: 판매자 2가지 등록 흐름(스크린샷/직접 입력), 관리자 Mock 흐름, 판매 희망금액 입력, 반응형 확인, typecheck, 설정되어 있다면 lint, production build, 콘솔 오류 확인
- Backend: 비로그인/관리자 권한, RLS, 제약조건, `docs/BACKEND.md`에서 정의한 동작 확인
- 전체 Playwright/E2E는 큰 Phase 완료, 여러 기능 통합, Frontend 전체 구현 완료, 또는 사용자의 명시적 요청 시 한 번 수행한다.
- 병렬 작업 중에는 각 작업자가 전체 Playwright를 실행하지 않는다. 통합 또는 Phase 완료 시 메인 작업자가 전체 검증을 수행한다.

### 배포 직전
production build/env, Supabase/Auth/RLS, 실제 판매 신청 1건, 관리자 조회·상태 변경, 모집 일시중지/종료, 모바일 사용성, 개인정보 노출 여부를 확인한다.

## Frontend 규칙
- 디자인 레퍼런스: https://www.codeit.kr
- 명확한 정보 위계, 충분한 여백, 강한 CTA, 정돈된 카드/폼, 짧은 카피를 참고하되 브랜드와 화면을 그대로 복제하지 않는다.
- 실제 시각 규칙은 `docs/DESIGN-SYSTEM.md`를 따른다.
- 판매자 화면은 mobile-first로 만들고 desktop에서도 자연스럽게 동작하게 한다.
- Frontend Phase에서는 판매 신청과 관리자 데이터를 Mock으로 사용한다. 스크린샷은 선택·미리보기까지만 구현하고 실제 영구 저장은 Backend Phase에서 연결한다.
- 상태는 공유가 꼭 필요한 경우가 아니면 로컬로 유지한다. Redux 같은 전역 상태관리는 기본 도입하지 않는다.

## Backend 원칙
- 기본 구조는 `React → Supabase` 직접 연결을 우선한다.
- 실제 필요가 생기지 않는 한 별도 Backend framework/server를 추가하지 않는다.
- Frontend 승인 후 승인된 화면과 데이터 요구사항을 기준으로 `docs/BACKEND.md`를 먼저 작성한다.
- 사용자가 Backend 문서 생성을 다시 요청할 필요가 없도록 다음 Phase 진입 시 AI가 스스로 진행한다.

## 사용 가능한 전문 에이전트
전문성이 결과를 실제로 바꾸는 시점에만 사용한다.
- `UI Designer`: 초기 디자인 방향 또는 중요한 UX 결정
- `Frontend Developer`: Frontend 구현
- `UI Finish-Gate Reviewer`: Frontend 완료 직전 1회
- `Experiment Tracker`: Backend 실험 필드와 지표 정의 시점
- `Identity & Access Engineer`: 관리자 Auth/RLS 구현 또는 검토
- `Privacy Engineer`: 연락처 등 개인정보 저장·노출 검토

위 목적이 아니라면 전문 에이전트를 호출하지 않는다.

## 범위 제한
다음은 이번 MVP에서 만들지 않는다.
- 구매자 기능 및 구매자 상품 목록
- 결제/PG, 자동 정산
- 판매자 회원가입/로그인
- 채팅
- QR OCR 및 중복 QR 탐지
- 이미지 OCR 및 자동 상품 판독
- 자동 상품 검색 및 자동 가격 최적화
- 판매자 등급
- 네이티브 앱

새로운 추상화, 의존성, 문서, 테스트 계층, 에이전트는 **현재 존재하는 문제를 해결할 때만** 추가한다.
